1、List：有顺序的线性存储，放入的元素可以重复。
		 适合实现有顺序要求的数据结构，如堆栈和队列
		 
2、map：元素按键值对存储，无放入顺序
		是将一个键值映射键值的对象，键不能相同，不能包含重复的键
		每个键最多映射到一个值
		 	 
		 
3、ArrayList是由数组实现List。
	动态数组，动态增加和减少元素，实现了ICollection和IList接口，灵活的设置数组的大小
	访问速度快，查找效率高